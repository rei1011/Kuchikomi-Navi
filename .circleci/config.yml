version: 2.1

jobs:
  say-hello:
    docker:
      - image: 'cimg/base:stable'
    steps:
      - run:
          name: Print Hello World
          command: echo "Hello, World!"

workflows:
  version: 2
  hello-world-workflow:
    jobs:
      - say-hello