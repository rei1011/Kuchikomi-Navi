"use client";

import { AppBar as CAppBar } from "@/component/AppBar";
import { Menu } from "@/component/Menu";

export const AppBar = () => {
  return (
    <CAppBar>
      <Menu />
    </CAppBar>
  );
};
