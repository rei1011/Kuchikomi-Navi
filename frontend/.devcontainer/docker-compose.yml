services:
  next-app:
    build:
      context: .
    volumes:
      - ../../:/usr/src/app:cached
    ports:
      - 3001:3001
    stdin_open: true
    tty: true
    networks:
      - kuchikomi-network

networks:
  kuchikomi-network:
    external: true
